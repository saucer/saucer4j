You can include these types in your TypeScript environment to better interact with Saucer's JS API.

Note that saucer does not currently auto-generate typescript definitions for your custom bindings. You will have to add those yourself.